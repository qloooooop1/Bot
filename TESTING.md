# دليل الاختبار السريع - بوت الأذكار الإسلامية

## اختبار البوت محلياً

### 1. اختبار وضع Long Polling

```bash
# تعيين متغير البيئة (اختياري)
export BOT_TOKEN="YOUR_BOT_TOKEN"

# تشغيل البوت
python App.py
```

البوت سيعمل في وضع polling ويستقبل الرسائل مباشرة.

### 2. اختبار وضع Webhook

```bash
# تعيين المتغيرات
export BOT_MODE=webhook
export BOT_TOKEN="YOUR_BOT_TOKEN"
export PORT=5000

# تشغيل البوت
python App.py
```

ثم في متصفح آخر أو باستخدام curl:

```bash
# تعيين webhook
curl "http://localhost:5000/setwebhook?url=https://your-domain.com"

# التحقق من webhook
curl "http://localhost:5000/webhookinfo"

# إزالة webhook
curl "http://localhost:5000/removewebhook"
```

## سيناريوهات الاختبار

### 1. إضافة البوت إلى مجموعة جديدة

1. أضف البوت إلى مجموعة تجريبية
2. اجعله مشرفاً
3. يجب أن يرسل رسالة ترحيب تلقائياً **ويُفعّل نفسه تلقائياً**
4. **جديد**: لم تعد هناك حاجة لأمر /enable!

### 2. اختبار أمر /start

#### في الخاص:
```
/start
```
يجب أن يعرض معلومات البوت مع أزرار:
- 👨‍💻 المطور (dev3bod)
- 👥 المجموعة الرسمية

#### في المجموعة (للمشرفين):
```
/start
```
يجب أن يعرض رسالة مع زر للوصول للإعدادات

### 3. عرض الحالة

```
/status
```

يجب أن يعرض:
- الحالة العامة (مفعّل/معطّل)
- الأوقات المجدولة
- المحتوى المتاح (100+ عنصر)

### 4. لوحة التحكم المحسّنة

```
/settings
```

يجب أن تظهر:
- أزرار تفاعلية محسّنة مع رموز تعبيرية
- معلومات الأوقات الحالية
- زر معلومات إضافية
- الفاصل الزمني الحالي

### 5. تخصيص الأوقات

```
/settime morning 06:30
/settime evening 19:00
/settime sleep 22:30
```

### 6. تخصيص الفاصل الزمني

```
/setinterval 60
```

لإرسال محتوى عشوائي كل 60 دقيقة.

### 7. اختبار المحتوى الخاص (جديد!)

```
/send_special ramadan
/send_special arafah
/send_special eid
/send_special last10
```

للمشرفين: يرسل محتوى خاص بالمناسبات الإسلامية

## الأوامر المتاحة للاختبار

### للجميع:
- `/start` - رسالة الترحيب (مختلفة في الخاص والمجموعات)
- `/help` - المساعدة

### للمشرفين:
- `/enable` - تفعيل البوت (في حال تم تعطيله)
- `/disable` - تعطيل البوت مؤقتاً
- `/status` - عرض الحالة المحسّنة
- `/settings` - لوحة التحكم المحسّنة
- `/settime <type> <HH:MM>` - تعديل الأوقات
- `/setinterval <minutes>` - تعديل الفاصل الزمني
- `/send_special <type>` - إرسال محتوى خاص (ramadan/arafah/eid/last10)

## التحقق من الجدولة

لاختبار أن الجدولة تعمل بشكل صحيح:

```python
# في Python shell
import App

# جدولة المهام
App.schedule_azkar_jobs()

# عرض المهام المجدولة
for job in App.scheduler.get_jobs():
    print(f"Job: {job.id}, Next run: {job.next_run_time}")
```

## تنظيف قاعدة البيانات للاختبار

```bash
# حذف قاعدة البيانات القديمة
rm azkar_bot.db

# إعادة تشغيل البوت
python App.py
```

سيتم إنشاء قاعدة بيانات جديدة بإعدادات افتراضية.

## ملاحظات مهمة

1. **التوكن**: تأكد من استخدام توكن صحيح من @BotFather
2. **الصلاحيات**: البوت يحتاج أن يكون مشرفاً في المجموعة
3. **التفعيل التلقائي**: البوت يُفعّل نفسه تلقائياً عند إضافته كمشرف (لا حاجة لـ /enable)
4. **التوقيت**: التوقيت الافتراضي هو Asia/Riyadh
5. **المحتوى الغني**: البوت يحتوي على أكثر من 100 دعاء وآية وحديث
6. **سورة الكهف**: تُرسل الجمعة 9:00 ص (تم تحديث التوقيت)
7. **الاختبار المحلي**: استخدم polling للاختبار المحلي
8. **الإنتاج**: استخدم webhook للنشر على خوادم الإنتاج

## استكشاف الأخطاء

### البوت لا يستجيب:
- تأكد من التوكن صحيح
- تأكد من البوت مشرف في المجموعة
- **جديد**: تأكد من حالة البوت باستخدام /status

### المهام المجدولة لا تعمل:
- تأكد من تشغيل scheduler
- تحقق من الأوقات المحددة باستخدام /status
- تأكد من تفعيل الميزات في /settings
- استخدم زر "🔄 حفظ وتطبيق التغييرات" بعد تعديل الإعدادات

### أخطاء قاعدة البيانات:
- احذف ملف azkar_bot.db
- أعد تشغيل البوت

## مثال على جلسة اختبار كاملة

```bash
# 1. تثبيت المتطلبات
pip install -r requirements.txt

# 2. تشغيل البوت
python App.py

# 3. في تيليجرام:
# - أضف البوت إلى مجموعة
# - اجعله مشرفاً
# - سيُفعّل تلقائياً! ✅
# - أرسل /start (كمشرف) لرؤية الأزرار
# - أرسل /settings لرؤية اللوحة المحسّنة
# - جرب تفعيل/تعطيل الميزات
# - اضغط على زر "ℹ️ معلومات الأوقات"
# - أرسل /status للتحقق

# 4. اختبار الأوامر المتقدمة:
/settime morning 06:00
/setinterval 120
/send_special ramadan
/status

# 5. اختبار /start في الخاص:
# - افتح محادثة خاصة مع البوت
# - أرسل /start
# - يجب أن تظهر أزرار المطور والمجموعة الرسمية

# 6. التحقق من أن كل شيء يعمل! ✅
```

## اختبار المحتوى الجديد

### التحقق من كمية المحتوى:
```python
import App

print(f"أذكار الصباح: {len(App.MORNING_AZKAR)}")  # يجب أن يكون 12+
print(f"أذكار المساء: {len(App.EVENING_AZKAR)}")  # يجب أن يكون 12+
print(f"الأدعية: {len(App.RANDOM_DUAS)}")  # يجب أن يكون 40+
print(f"الآيات: {len(App.QURAN_VERSES)}")  # يجب أن يكون 40+
print(f"الأحاديث: {len(App.HADITH_TEXTS)}")  # يجب أن يكون 40+
print(f"أدعية الجمعة: {len(App.FRIDAY_DUAS)}")  # 5+
print(f"أدعية رمضان: {len(App.RAMADAN_DUAS)}")  # 5
print(f"أدعية عرفة: {len(App.ARAFAH_DUAS)}")  # 3
print(f"أدعية العيد: {len(App.EID_DUAS)}")  # 3
print(f"أدعية العشر الأواخر: {len(App.LAST_TEN_DAYS_DUAS)}")  # 3
```

### اختبار التفعيل التلقائي:
1. أضف البوت لمجموعة جديدة كمشرف
2. تحقق من استلامك لرسالة "تم تفعيل البوت تلقائياً! ✅"
3. استخدم /status للتحقق من الحالة (يجب أن يكون 🟢 مفعّل)

### اختبار سيناريو الفشل (البوت ليس مشرفاً):
1. أضف البوت لمجموعة جديدة **بدون** جعله مشرفاً
2. يجب أن تظهر رسالة تطلب جعل البوت مشرفاً
3. اجعل البوت مشرفاً
4. يجب أن يُفعّل تلقائياً ويرسل رسالة تأكيد
5. استخدم /status للتحقق من التفعيل

