# دليل الإعدادات المتقدمة - Advanced Settings Guide

## نظرة عامة / Overview

تم إضافة نظام إعدادات متقدم لبوت نور الذكر يتيح للمستخدمين التحكم الكامل في:
- إرسال الوسائط (صور، فيديو، ملفات) مع الأذكار
- تخصيص مواعيد إرسال الأذكار
- التحكم في أنواع المحتوى المرسل

An advanced settings system has been added to Noor Al-Dhikr bot that allows users to fully control:
- Sending media (images, videos, files) with azkar
- Customizing azkar sending schedules
- Controlling content types sent

---

## الوصول إلى الإعدادات المتقدمة / Accessing Advanced Settings

### للمشرفين / For Admins

1. ابدأ محادثة خاصة مع البوت / Start a private chat with the bot
2. أرسل الأمر `/start`
3. اضغط على زر "⚙️ إعدادات البوت"
4. اضغط على زر "⚙️ الإعدادات المتقدمة"

---

## إعدادات الوسائط / Media Settings

### تفعيل الوسائط / Enabling Media

لتفعيل إرسال الوسائط مع الأذكار في مجموعة معينة:

To enable media sending with azkar in a specific group:

1. اذهب إلى المجموعة / Go to the group
2. أرسل الأمر `/settings`
3. **ملاحظة:** ميزة تبديل الوسائط ستتوفر قريباً في واجهة المستخدم

**Note:** Media toggle features will be available soon in the UI

### أنواع الوسائط المتاحة / Available Media Types

- 📸 **صور / Images**: صور إسلامية وخلفيات
- 🎥 **فيديو / Videos**: مقاطع فيديو قصيرة
- 📄 **ملفات / Documents**: ملفات PDF والمستندات
- 🎲 **عشوائي / Random**: اختيار عشوائي من جميع الأنواع

### إضافة وسائط جديدة / Adding New Media

#### الطريقة 1: تحديث ملف JSON / Method 1: Update JSON File

1. افتح ملف `media_database.json`
2. أضف عنصر جديد في الفئة المناسبة:

```json
{
  "id": "img_004",
  "type": "images",
  "file_id": "AgACAgQAAxkBAAIC...",
  "description": "وصف الصورة",
  "tags": ["علامة1", "علامة2"],
  "category": "islamic_art",
  "enabled": true
}
```

3. احفظ الملف

#### الطريقة 2: استخدام دالة البرمجة / Method 2: Use Programming Function

```python
from App import update_media_database

new_media = {
    "id": "vid_003",
    "type": "videos",
    "file_id": "BAACAgQAAxkBAAIC...",
    "description": "مقطع تلاوة قرآنية",
    "tags": ["قرآن", "تلاوة"],
    "category": "quran",
    "enabled": True
}

update_media_database(new_media)
```

### الحصول على file_id من تيليجرام / Getting file_id from Telegram

لإضافة وسائط جديدة، تحتاج إلى `file_id` من تيليجرام:

To add new media, you need the `file_id` from Telegram:

1. أرسل الصورة/الفيديو/الملف للبوت
2. استخدم Telegram Bot API للحصول على file_id
3. أو استخدم بوت @userinfobot للحصول على معلومات الملف

**مثال / Example:**
```
AgACAgQAAxkBAAICBWZxX1K...  (صورة / photo)
BAACAgQAAxkBAAICCmZxX2L...  (فيديو / video)
BQACAgQAAxkBAAICDWZxX3M...  (ملف / document)
```

---

## إعدادات المواعيد / Schedule Settings

### تغيير أوقات الإرسال / Changing Send Times

استخدم الأمر `/settime` في المجموعة:

Use the `/settime` command in the group:

```
/settime <type> <time>
```

#### أنواع الأذكار المتاحة / Available Azkar Types

- `morning` - أذكار الصباح (الافتراضي: 05:00)
- `evening` - أذكار المساء (الافتراضي: 18:00)
- `sleep` - رسالة النوم (الافتراضي: 22:00)

#### أمثلة / Examples

```bash
# تغيير وقت أذكار الصباح إلى 6:30 صباحاً
/settime morning 06:30

# تغيير وقت أذكار المساء إلى 7:00 مساءً
/settime evening 19:00

# تغيير وقت رسالة النوم إلى 11:00 مساءً
/settime sleep 23:00
```

### الأوقات الثابتة / Fixed Times

بعض الأذكار لها أوقات ثابتة لا يمكن تغييرها:

Some azkar have fixed times that cannot be changed:

- 📿 سورة الكهف: كل جمعة 09:00
- 🕌 دعاء الجمعة: كل جمعة 10:00

---

## قاعدة بيانات الوسائط / Media Database

### هيكل قاعدة البيانات / Database Structure

```json
{
  "version": 1,
  "description": "قاعدة بيانات الوسائط",
  "note": "ملاحظة: يجب ملء file_id بمعرف الملف الفعلي",
  "media": {
    "images": [...],
    "videos": [...],
    "documents": [...]
  },
  "settings": {
    "default_media_type": "images",
    "random_selection": true,
    "media_with_azkar": true
  }
}
```

### الحقول المطلوبة / Required Fields

كل عنصر وسائط يجب أن يحتوي على:

Each media item must contain:

- `id`: معرف فريد / Unique identifier
- `type`: نوع الوسائط / Media type (images/videos/documents)
- `file_id`: معرف الملف من تيليجرام / Telegram file ID
- `description`: وصف الوسائط / Media description
- `tags`: علامات للبحث / Tags for search (array)
- `category`: الفئة / Category
- `enabled`: حالة التفعيل / Enabled status (true/false)

---

## أمثلة الاستخدام / Usage Examples

### مثال 1: إعداد بوت جديد / Example 1: Setting Up New Bot

```bash
1. إضافة البوت إلى المجموعة كمشرف
2. إرسال /start في الخاص
3. الضغط على "⚙️ إعدادات البوت"
4. الضغط على "⚙️ الإعدادات المتقدمة"
5. اختيار نوع الوسائط المفضل
6. العودة للمجموعة وإرسال /settime morning 06:00
```

### مثال 2: إضافة صورة إسلامية / Example 2: Adding Islamic Image

```json
{
  "id": "img_masjid_nabawi",
  "type": "images",
  "file_id": "AgACAgQAAxkBAAICBWZxX1K...",
  "description": "المسجد النبوي الشريف",
  "tags": ["مسجد", "المدينة", "النبوي"],
  "category": "holy_places",
  "enabled": true
}
```

### مثال 3: جدولة أوقات مخصصة / Example 3: Custom Schedule

```bash
# في مجموعة تعمل ليلاً
/settime morning 07:00
/settime evening 20:00
/settime sleep 02:00
```

---

## حل المشاكل / Troubleshooting

### الوسائط لا تُرسل / Media Not Sending

**المشكلة:** الوسائط لا تظهر مع الأذكار

**الحلول:**
1. تحقق من أن `file_id` ليس فارغاً
2. تحقق من أن `enabled` = `true`
3. تحقق من صلاحيات البوت في المجموعة
4. تحقق من أن نوع الوسائط صحيح (images/videos/documents)

### الأوقات لا تعمل / Times Not Working

**المشكلة:** الأذكار لا تُرسل في الوقت المحدد

**الحلول:**
1. تحقق من صيغة الوقت (HH:MM)
2. تحقق من المنطقة الزمنية (افتراضياً: Asia/Riyadh)
3. أعد جدولة بإرسال `/settime` مرة أخرى
4. تحقق من تفعيل البوت في المجموعة

### خطأ في file_id / file_id Error

**المشكلة:** خطأ عند إرسال الوسائط

**الحلول:**
1. تأكد من أن `file_id` صحيح ومن نفس البوت
2. تأكد من أن الملف لا يزال موجوداً على خوادم تيليجرام
3. جرب إعادة رفع الملف والحصول على `file_id` جديد

---

## الأمان / Security

### نصائح الأمان / Security Tips

1. ✅ لا تشارك `file_id` للوسائط الخاصة
2. ✅ احتفظ بنسخة احتياطية من `media_database.json`
3. ✅ فعّل فقط الوسائط الموثوقة
4. ✅ راجع الوسائط قبل التفعيل

### صلاحيات البوت المطلوبة / Required Bot Permissions

- إرسال الرسائل / Send messages
- إرسال الصور / Send photos
- إرسال الفيديو / Send videos
- إرسال الملفات / Send documents
- حذف الرسائل (اختياري) / Delete messages (optional)

---

## الدعم الفني / Technical Support

للمساعدة أو الإبلاغ عن مشاكل:

For help or to report issues:

- 👨‍💻 المطور / Developer: [@dev3bod](https://t.me/dev3bod)
- 👥 المجموعة الرسمية / Official Group: [@NourAdhkar](https://t.me/NourAdhkar)
- 🐛 GitHub Issues: [qloooooop1/Bot](https://github.com/qloooooop1/Bot/issues)

---

## الترخيص / License

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

This project is open source and available for free use.

---

**آخر تحديث / Last Updated:** 2026-01-15
