# دليل النشر على Render - Islamic Azkar Bot

## خطوات النشر التفصيلية

### 1. التحضير

قبل البدء، تأكد من أن لديك:
- حساب GitHub مع المستودع الخاص بك
- توكن البوت من [@BotFather](https://t.me/BotFather)
- معرف المجموعة التي سيعمل فيها البوت

### 2. الحصول على معرف المجموعة

1. أضف البوت [@userinfobot](https://t.me/userinfobot) إلى مجموعتك
2. سيرسل لك معرف المجموعة (Chat ID)
3. احفظ هذا الرقم (سيكون بصيغة سالبة مثل: -1001234567890)

### 3. إنشاء حساب على Render

1. اذهب إلى [Render.com](https://render.com)
2. انقر على "Get Started" أو "Sign Up"
3. سجل باستخدام حساب GitHub الخاص بك

### 4. إنشاء خدمة ويب جديدة

1. من لوحة تحكم Render، انقر على "New +"
2. اختر "Web Service"
3. قم بربط حساب GitHub إذا لم يكن مربوطاً
4. اختر المستودع `Bot`
5. أدخل الإعدادات التالية:

   - **Name**: `islamic-azkar-bot` (أو أي اسم تفضله)
   - **Region**: اختر المنطقة الأقرب لك
   - **Branch**: `main` (أو الفرع الذي تعمل عليه)
   - **Runtime**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `gunicorn App:app`

### 5. إضافة متغيرات البيئة

في قسم "Environment Variables"، أضف المتغيرات التالية:

| Key | Value | مثال |
|-----|-------|------|
| `BOT_TOKEN` | توكن البوت من BotFather | `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11` |
| `ALLOWED_CHAT_ID` | معرف المجموعة | `-1001224326322` |
| `WEBHOOK_URL` | سيتم ملؤه بعد الإنشاء | `https://islamic-azkar-bot.onrender.com` |

**ملاحظة**: اترك `WEBHOOK_URL` فارغاً في البداية

### 6. إنشاء الخدمة

1. انقر على "Create Web Service"
2. انتظر حتى يتم بناء ونشر الخدمة (قد يستغرق 2-3 دقائق)
3. بعد النشر، ستحصل على رابط مثل: `https://islamic-azkar-bot.onrender.com`

### 7. تحديث متغير WEBHOOK_URL

1. انسخ رابط الخدمة من Render
2. ارجع إلى "Environment" في إعدادات الخدمة
3. أضف أو عدل `WEBHOOK_URL` بالرابط الذي حصلت عليه
4. احفظ التغييرات (ستعيد Render نشر الخدمة تلقائياً)

### 8. تفعيل Webhook

1. افتح رابط الخدمة في المتصفح: `https://islamic-azkar-bot.onrender.com`
2. يجب أن ترى رسالة: "البوت جاهز والـ webhook مُعيَّن! 🚀"
3. هذا يعني أن البوت الآن يعمل ومتصل بتليجرام

### 9. اختبار البوت

1. اذهب إلى مجموعة تليجرام
2. أضف البوت إلى المجموعة
3. اجعله مشرفاً مع الصلاحيات التالية:
   - حذف الرسائل
   - حظر المستخدمين
   - تثبيت الرسائل (اختياري)

4. أرسل الأمر `/admin` في المجموعة
5. يجب أن تظهر لك لوحة التحكم

### 10. الإعدادات الأولية

من لوحة التحكم (`/admin`):
1. فعّل نشر الأذكار
2. اختر الفاصل الزمني المناسب
3. فعّل/عطّل حذف رسائل الخدمة حسب حاجتك
4. اختبر نشر ذكر بالضغط على "نشر ذكر الآن"

## ملاحظات مهمة

### خطة Render المجانية
- الخدمة المجانية تتوقف بعد 15 دقيقة من عدم النشاط
- ستعود للعمل تلقائياً عند استلام طلب جديد
- للبوتات النشطة، يُنصح بترقية للخطة المدفوعة

### صلاحيات البوت
تأكد من إعطاء البوت هذه الصلاحيات:
- ✅ Delete Messages
- ✅ Ban Users
- ✅ Pin Messages (إذا كنت تريد حذف إشعارات التثبيت)
- ✅ Invite Users via Link (اختياري)

### مراقبة السجلات
لرؤية سجلات البوت:
1. اذهب إلى لوحة تحكم Render
2. اختر خدمتك
3. انقر على "Logs"
4. ستشاهد جميع الأحداث والأخطاء

## حل المشاكل الشائعة

### البوت لا يستجيب
1. تحقق من أن الخدمة تعمل على Render (Status: Live)
2. تحقق من متغيرات البيئة
3. راجع السجلات للبحث عن أخطاء
4. تأكد من أن البوت مشرف في المجموعة

### Webhook لا يعمل
1. افتح رابط الخدمة في المتصفح مرة أخرى
2. تحقق من أن `WEBHOOK_URL` صحيح
3. استخدم BotFather لإعادة تعيين webhook

### الأذكار لا تُنشر
1. تحقق من أن الميزة مفعّلة في لوحة التحكم
2. تأكد من أن ملف `azkar_data.json` موجود
3. راجع السجلات للبحث عن أخطاء

## التحديثات المستقبلية

عند دفع تحديثات جديدة للمستودع:
1. Render سيكتشف التغييرات تلقائياً
2. سيعيد بناء ونشر الخدمة
3. لن تحتاج لإعادة تكوين أي شيء

## الدعم

إذا واجهت مشاكل:
1. راجع السجلات على Render
2. تحقق من Issues على GitHub
3. افتح Issue جديد مع تفاصيل المشكلة

---

بالتوفيق! 🚀
