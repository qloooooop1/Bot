# 🌟 بوت الأذكار الإسلامية 🌟

بوت تيليجرام متقدم لإرسال الأذكار والأدعية الإسلامية تلقائياً في المجموعات.

## ✨ المميزات

### 📿 الأذكار المجدولة
- **أذكار الصباح**: تُرسل تلقائياً في الساعة 5:00 صباحاً (قابلة للتخصيص)
- **أذكار المساء**: تُرسل تلقائياً في الساعة 6:00 مساءً (قابلة للتخصيص)
- **سورة الكهف**: تُرسل يوم الخميس في الساعة 11:00 صباحاً (قبل صلاة الجمعة)
- **أدعية الجمعة**: تُرسل يوم الجمعة في الساعة 10:00 صباحاً
- **رسالة قبل النوم**: تُرسل في الساعة 10:00 مساءً (قابلة للتخصيص)

### 🎲 المحتوى المتنوع
- أدعية عشوائية من الأدعية المأثورة
- آيات قرآنية منسقة بشكل جميل
- أحاديث نبوية شريفة
- فاصل زمني قابل للتخصيص بين المحتويات (افتراضي: 180 دقيقة)

### ⚙️ لوحة تحكم شاملة
- تفعيل/تعطيل كل ميزة بشكل مستقل
- تخصيص أوقات إرسال الأذكار
- التحكم في الفاصل الزمني للمحتوى العشوائي
- واجهة سهلة الاستخدام بالأزرار التفاعلية

### 🚀 التشغيل التلقائي
- يعمل تلقائياً على أي مجموعة يكون البوت مشرفاً فيها
- دعم كامل لـ **Long Polling** (للتطوير والاختبار)
- دعم كامل لـ **Webhook** (للإنتاج)
- لا يحتاج إلى إدخال معرف المجموعة يدوياً

## 📋 المتطلبات

```
pyTelegramBotAPI
flask
APScheduler
pytz
Pillow
```

## 🔧 التثبيت والإعداد

### 1. تثبيت المتطلبات

```bash
pip install -r requirements.txt
```

### 2. إعداد التوكن

قم بإنشاء بوت جديد عبر [@BotFather](https://t.me/BotFather) واحصل على التوكن.

يمكنك إما:
- تعديل التوكن مباشرة في ملف `App.py`
- أو تعيينه كمتغير بيئة:

```bash
export BOT_TOKEN="YOUR_BOT_TOKEN_HERE"
```

### 3. تشغيل البوت

#### وضع Long Polling (التطوير):

```bash
python App.py
```

أو:

```bash
export BOT_MODE=polling
python App.py
```

#### وضع Webhook (الإنتاج):

```bash
export BOT_MODE=webhook
export PORT=5000
python App.py
```

ثم قم بتعيين webhook عبر زيارة:
```
http://your-domain.com/setwebhook?url=https://your-domain.com
```

## 📱 كيفية الاستخدام

### إضافة البوت إلى مجموعة

1. أضف البوت إلى مجموعتك
2. اجعله مشرفاً (Admin)
3. البوت سيرحب بكم تلقائياً

### تفعيل البوت

```
/enable
```

### الأوامر المتاحة

#### للجميع:
- `/start` - رسالة الترحيب والمساعدة
- `/help` - عرض المساعدة

#### للمشرفين فقط:
- `/enable` - تفعيل البوت في المجموعة
- `/disable` - تعطيل البوت
- `/status` - عرض حالة البوت والإعدادات الحالية
- `/settings` - فتح لوحة التحكم التفاعلية
- `/settime <نوع> <الوقت>` - تغيير وقت إرسال الأذكار
- `/setinterval <دقائق>` - تغيير الفاصل الزمني للمحتوى العشوائي

### أمثلة على الأوامر

تغيير وقت أذكار الصباح:
```
/settime morning 06:00
```

تغيير وقت أذكار المساء:
```
/settime evening 19:00
```

تغيير وقت رسالة النوم:
```
/settime sleep 23:00
```

تغيير الفاصل الزمني إلى ساعتين (120 دقيقة):
```
/setinterval 120
```

## 🎨 التنسيق والرموز التعبيرية

جميع الرسائل منسقة بشكل جميل مع:
- ✨ رموز تعبيرية إسلامية مناسبة
- 📿 تنسيق واضح وسهل القراءة
- 🌙 ثيمات دينية جذابة

## 🔒 الأمان والخصوصية

- لا يتم تخزين محتوى الرسائل
- يتم تخزين إعدادات المجموعات فقط في قاعدة بيانات محلية
- يعمل فقط في المجموعات التي تم تفعيله فيها صراحة

## 🌐 النشر

### Vercel

1. قم بإنشاء حساب على [Vercel](https://vercel.com)
2. اربط مستودع GitHub الخاص بك
3. أضف متغيرات البيئة:
   - `BOT_TOKEN`: توكن البوت
   - `BOT_MODE`: `webhook`
4. انشر المشروع

### Heroku

1. قم بإنشاء تطبيق جديد على Heroku
2. أضف buildpack: `heroku/python`
3. أضف متغيرات البيئة
4. انشر الكود

### VPS/Server

```bash
# تثبيت المتطلبات
pip install -r requirements.txt

# تشغيل بوضع polling (في الخلفية)
nohup python App.py &

# أو استخدم supervisor/systemd للتشغيل الدائم
```

## 📊 الإحصائيات

البوت يستخدم APScheduler لجدولة المهام بدقة مع دعم:
- ⏰ جدولة دقيقة بناءً على التوقيت المحلي
- 🔄 إعادة جدولة تلقائية عند تغيير الإعدادات
- 💪 معالجة قوية للأخطاء

## 🤝 المساهمة

نرحب بالمساهمات! يمكنك:
- إضافة محتوى جديد (أذكار، أدعية، آيات)
- تحسين التنسيق والواجهة
- إصلاح الأخطاء
- إضافة ميزات جديدة

## 📝 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

## 🤲 دعاء

نسأل الله أن ينفع بهذا البوت، وأن يجعله في ميزان حسنات كل من ساهم فيه أو استخدمه.

---

**تم التطوير بـ ❤️ لخدمة الإسلام والمسلمين**
